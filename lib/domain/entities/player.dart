import 'package:flutter/material.dart';

class Player {
  Offset offset;
  double radius;
  Path shape;

  Player({
    required this.offset,
    required this.radius,
    required this.shape,
  });
}
